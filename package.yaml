apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: krateo-template-fireworksapp
spec:
  package: "ghcr.io/krateoplatformops/krateo-template-fireworksapp:latest"
  packagePullPolicy: Always
  revisionActivationPolicy: Automatic
  revisionHistoryLimit: 1
